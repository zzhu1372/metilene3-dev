README file for metilene

INSTALL
==============
1. Get python packages
pip install pandas matplotlib biopython

2. Compile
python /path_to_metilene/metilene.py -compile
or 
cd /path_to_metilene;make

RUN METILENE
==============
python /path_to_metilene/metilene.py -i {input} -o ./{output_dir}
