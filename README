README file for metilene


INSTALL
==============
1. Get Python3 packages
pip install pandas matplotlib biopython gseapy
or
pip3 install pandas matplotlib biopython gseapy

2. Get R packages
Run the following in R:
'''
if (!require("BiocManager", quietly = TRUE))
    install.packages("BiocManager")
BiocManager::install("ChIPseeker")
'''

Tips:
   93  sudo apt install libssl-dev
   94  sudo apt install libxml2-dev
   96  sudo apt install libfontconfig1-dev
   98  sudo apt install libcurl4-openssl-dev
  153  sudo apt update
  154  sudo apt install --no-install-recommends r-base
  155  sudo apt install --no-install-recommends r-base-dev




3. Compile
cd /path_to_metilene;make


RUN METILENE
==============
python3 /path_to_metilene/metilene.py -i {input} -o ./{output_dir}
